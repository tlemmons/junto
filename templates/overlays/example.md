# {{project}} — project-specific rules

(This is a starter overlay file. Copy to `overlays/_yourproject.md` and edit.
For background on overlay structure and rules, see `templates/README.md`.)

DELETE THIS HEADER WHEN YOU CUSTOMIZE.

---

## Team roster

This project has these peers:

- `coordinator@{{project}}` — routes cross-team work, owns architectural calls.
- (add your real peer list here)

Default route: peer-to-peer. Cross-team work: through coordinator.

## Build commands

This project uses [your-tool-here]. Build: `[command]`. Test: `[command]`.

## Project Q1 escalation

Any decision touching two teams' code goes to coordinator before you act,
not after.
